pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Test Clarusway_Way to Reinvent Yourself go get it'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}
